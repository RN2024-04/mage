# 1
from PIL import Image

# Читать изображения с помощью Pillow
# Использовать Pilow для обработки изображений
# Используйте NumPy с Pillow для дальнейшей обработки
# Создавать анимации с помощью Pillow

im=Image.open('photo.jpg')
new=im.resize((100,100))
new.show()


# 2
import requests

# С помощью этого объекта можно получить всю необходимую информацию.

response = requests.get('https://www.example.com')
print(response.text)

# 3
import numpy as np

# реализует операции линейной алгебры (простое умножение векторов и матриц есть в базовом варианте);
# реализует функции для работы со случайными величинами;
# реализует прямое и обратное преобразование Фурье.

list1 = [2, 4, 6, 8]
array1 = np.array(list1)
print(array1)
